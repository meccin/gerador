---
import ThemeToggle from '@/components/ThemeToggle'
import MobileMenu from '@/components/MobileMenu'

const links = [
  { href: '/', label: 'CPF / CNPJ / RG' },
  { href: '/credit-card', label: 'Cartão de Crédito' },
  { href: '/json-pretty', label: 'JSON Pretty' },
  { href: '/base64-encode-decode', label: 'Base64' },
]

const currentPath = Astro.url.pathname
---

<header class="border-b border-border bg-background/95 backdrop-blur-sm sticky top-0 z-50">
  <div class="max-w-4xl mx-auto px-4 h-14 flex items-center justify-between">
    <a href="/" class="font-bold text-xl text-foreground hover:text-foreground/80 transition-colors">
      Gerador
    </a>

    <!-- Desktop nav -->
    <nav class="hidden md:flex items-center gap-5">
      {links.map(({ href, label }) => (
        <a
          href={href}
          class:list={[
            'text-sm font-medium transition-all underline decoration-dashed underline-offset-4',
            currentPath === href
              ? 'text-foreground decoration-primary'
              : 'text-muted-foreground decoration-transparent hover:text-foreground hover:decoration-primary',
          ]}
        >
          {label}
        </a>
      ))}
    </nav>

    <div class="flex items-center gap-2">
      <ThemeToggle client:load />
      <div class="md:hidden">
        <MobileMenu links={links} client:visible />
      </div>
    </div>
  </div>
</header>
