export enum DocumentType {
  CPF = 'CPF',
  CNPJ = 'CNPJ',
  RG = 'RG',
}

export enum CreditCardBrand {
  AMEX = 'amex',
  MASTERCARD = 'mastercard',
  VISA = 'visa',
}

export enum HashAlgorithm {
  MD5 = 'MD5',
  SHA1 = 'SHA-1',
  SHA256 = 'SHA-256',
  SHA384 = 'SHA-384',
  SHA512 = 'SHA-512',
}
